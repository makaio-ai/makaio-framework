/**
 * Category key for last scan timestamp in preferences storage.
 * Separate from settings to avoid read-modify-write race conditions.
 */
export const IMPORT_LAST_SCAN_CATEGORY = 'import-last-scan';
