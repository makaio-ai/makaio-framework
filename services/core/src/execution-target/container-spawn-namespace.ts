import { DockerNamespace, DockerSubjects } from './container-namespace.js';

/**
 * Backward-compatible alias for the canonical Docker namespace.
 */
export const ContainerSpawnNamespace = DockerNamespace;

/**
 * Backward-compatible alias for the canonical Docker subjects.
 */
export const ContainerSpawnSubjects = DockerSubjects;
