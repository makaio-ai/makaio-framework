---
"@makaio/framework": patch
"@makaio/sdk": patch
---

**@makaio/framework**
- `ws` unified to `^8.20.1` across root, runtimes, SDKs, and transport (dependencies and peerDependencies). `vite` bumped to `^8.0.13` in build-tooling and affected workspaces.
- App-specific dependency bumps: `@vitejs/plugin-react` (6.0.1→6.0.2), `vite` bumps in apps, `astro` (6.3.1→6.3.3), `starlight-llms-txt` (0.8.1→0.10.0), and `react-resizable-panels` (4.4.1→4.11.1); root devDependencies adjusted.

**@makaio/sdk**
- `ws` unified to `^8.20.1` across root, runtimes, SDKs, and transport (dependencies and peerDependencies). `vite` bumped to `^8.0.13` in build-tooling and affected workspaces.
