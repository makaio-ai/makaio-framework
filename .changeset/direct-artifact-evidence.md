---
"@makaio/contracts": major
"@makaio/framework": major
---

Add strict, immutable direct evidence citations to artifact revisions, including pinned Git files, versioned Confluence pages, and exact artifact revisions with source-specific locations. Require artifact revision references to carry an explicit `artifact` reference class and reject blank coordinates or undeclared fields.
