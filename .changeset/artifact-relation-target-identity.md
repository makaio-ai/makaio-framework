---
'@makaio/contracts': minor
---

Add `ArtifactRelationTargetIdentitySchema`, `ArtifactRelationTargetIdentity`, and `artifactRelationTargetIdentity` to publish the pin-less relation target identity once in the contracts, removing the need for consumers to re-implement it.
