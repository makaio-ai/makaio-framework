---
'@makaio/framework': patch
'@makaio/runtime-node': patch
---

Provide the required CLI detection handler in framework Node and isolated
workflow runtimes so client scans no longer depend on product packages.
