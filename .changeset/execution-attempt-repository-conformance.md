---
"@makaio/framework": minor
---

Publish a callable ExecutionAttemptRepository contract suite under workflow-engine/testing/conformance so host persistence implementations can run the same behavioral checks as the reference repositories. The suite uses the optional Vitest peer; the existing reference implementation imports remain independent of Vitest.
