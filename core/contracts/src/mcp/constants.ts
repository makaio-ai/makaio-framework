/** Tool name string constants for the MCP discovery meta-tools. */

/** Tool name for invoking an MCP tool by full name. */
export const MCP_CALL_TOOL_NAME = 'mcp_call' as const;

/** Tool name for discovering available MCP tools. */
export const MCP_DISCOVER_TOOL_NAME = 'mcp_discover' as const;

/** Tool name for retrieving MCP tool input schema. */
export const MCP_SCHEMA_TOOL_NAME = 'mcp_schema' as const;
