/**
 * Local adapter-subsystem subject re-export.
 *
 * The service package uses the shared framework contracts namespace directly so
 * the service and its consumers stay on the same subject definitions.
 */
export { AdapterSubsystemNamespace, AdapterSubsystemSubjects } from '@makaio/services-core/adapter-subsystem';
