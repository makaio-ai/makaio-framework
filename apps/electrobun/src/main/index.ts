/**
 * Framework-default Electrobun main-process entry point.
 *
 * Host-specific behavior enters through runtime config, descriptors,
 * package exports, or typed host seams rather than build-time entry overrides.
 */
import './main.js';
