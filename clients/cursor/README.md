# @makaio/client-cursor

Static client definition for Cursor's command-line client within the Makaio framework.

The package exposes the client identity metadata used by adapters and host runtime discovery.
